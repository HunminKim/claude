# verifier 임시 결과 파일 (훅이 처리 후 자동 삭제)
.verifier_result.json

# 훅 상태 (사용자별 flag 파일 — ruff_warned, plan_gate_intro_seen 등)
.claude/state/

# 환경 변수 / 시크릿
.env

# prompt-log 런타임 수집 데이터 — 정식 위치는 ~/.claude/prompt-log/ (저장소 커밋 금지)
# 루트 고정(/) — plugins/prompt-log/ 는 플러그인 코드라 제외
/prompt-log/

# OS
.DS_Store
Thumbs.db
__pycache__/
