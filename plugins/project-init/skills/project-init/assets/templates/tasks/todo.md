# 작업 계획 (Todo)

> 3단계 이상 복잡한 작업 시작 전 작성. 완료 후 삭제하거나 초기화.
> **위임 프롬프트의 단일 진실 원천** — 메인 Claude는 서브에이전트 위임 시 아래 5섹션을 그대로 발췌해 전달한다.

---

## 현재 작업

**목표**: 

**예상 단계**:
- [ ] 1.
- [ ] 2.
- [ ] 3.

---

## 영향 파일

> 이 Step에서 수정·생성·삭제할 파일 목록. 위임 시 그대로 발췌해 서브에이전트 프롬프트의 "담당 파일 범위"에 넣는다.
> 비어 있으면 "N/A — 새 파일만 생성" 등으로 명시. **블록 자체는 누락 금지**.

- 

---

## USER_DECISIONS

> 사용자가 명시 선택한 결정. **자유도 0**. 변경·우회·차선책 자체 선택 모두 금지.
> AskUserQuestion 응답을 받자마자 여기에 박는다. 평문에 섞지 않는다.
> 비어 있으면 "N/A" 한 줄로 명시. **블록 자체는 누락 금지**.

- 

---

## CONSTRAINTS

> 이번 Step의 일반 제약 (성능 한계, 보안 규칙, 호환성 요구 등).
> 막히면 즉시 메인 보고. 자체 해결 시도 금지.
> 비어 있으면 "N/A".

- 

---

## 기술 충돌 점검

> 위임 전 호환성·동시성·인터페이스 충돌 가능성을 점검한다. Plan subagent 외부 검증의 입력으로 사용된다.

- [ ] 동기/비동기 메서드 일치 (예: async MCP 도구 vs sync caller)
- [ ] 외부 MCP·SDK 호출 방식 호환 확인
- [ ] 공유 타입·인터페이스 변경 시 다른 모듈 영향 추적
- [ ] 의존성 버전 충돌 점검
- [ ] 추가 항목:

---

<!-- FALLBACK BEGIN -->
## fallback

> 1순위 경로가 실패할 때만 사용하는 우회 경로. **별도 게이트** — 1순위 성공 시 진입 금지.
> HTML 주석 마커(`<!-- FALLBACK BEGIN/END -->`)로 감싸 위임 프롬프트 발췌 시 메인이 의도적으로 포함·제외한다.
> 비어 있으면 "N/A" 한 줄로 명시.

- 
<!-- FALLBACK END -->

---

## 검토

**완료 기준** (기계적 판별 가능하게):
- [ ] 기능 정합성: 
- [ ] **운영 규모 검증**: 실제 입력 규모(데이터 건수/파일 크기/반복 횟수)에서 메모리·시간·리소스를 1회 이상 실측. 소규모 sanity check 만으로 완료 판정 금지.
- [ ] 에러/경계 입력 처리: 

**결과 요약**: 
