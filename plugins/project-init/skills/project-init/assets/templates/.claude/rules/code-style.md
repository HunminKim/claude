---
description: 코드 작성 규칙 — 해당 언어 파일 편집 시 조건부 로드
paths:
  - "**/*.py"
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "**/*.go"
  - "**/*.rs"
  - "**/*.java"
  - "**/*.cpp"
  - "**/*.c"
---

# 코드 작성 규칙

## 언어
- 문서·주석은 한국어 (라이브러리명·API명·고유명사는 영어 유지)

## 작업 프로세스
- 작업 전 요구사항/제약/입출력을 5~10줄 요약
- 요구사항 외 기능 추가 금지 (필요 시 "추가 제안"으로 분리)
- 불명확한 부분은 Assumption으로 명시
- 상수·enum·config·schema 등 정의부를 반드시 먼저 확인 후 인용 (확인 없이 수정 금지)
- 임시방편 금지 — 근본 원인을 찾는다. 시니어 엔지니어 기준 유지
- 필요한 것만 변경 — 불필요한 변경은 버그 도입 경로가 된다

## 단순성 우선 (Simplicity First)

- 요청한 것만 구현한다. 요청하지 않은 기능·옵션·유연성은 추가하지 않는다.
- 단일 용도 코드에 추상화 계층을 만들지 않는다 (클래스·인터페이스·헬퍼 함수를 "나중을 위해" 만들지 않는다).
- 발생 불가능한 시나리오에 에러 핸들링을 추가하지 않는다.
- 200줄로 쓴 코드가 50줄로 줄어든다면, 50줄로 다시 쓴다.
- 구현 전 자문: "시니어 엔지니어가 이 코드를 보고 과하다고 할까?" — 그렇다면 줄인다.
- 예외: 요청에 "확장 가능하게", "유연하게", "범용으로" 등의 명시가 있을 때만 추상화 허용.

## 외과적 변경 원칙 (Surgical Changes)

- **인접 코드 불가침**: 수정 대상 주변의 주석·포맷·불필요해 보이는 코드를 함께 정리하지 않는다
- **스타일 매칭**: 기존 코드 스타일이 마음에 들지 않아도 그대로 따른다. 스타일 통일은 별도 요청 시에만
- **기존 데드코드 불가침**: 이미 있던 미사용 import·함수·변수는 언급만 — 삭제는 명시 요청 시에만
- **자신이 만든 고아는 즉시 정리**: 이번 변경으로 새로 생긴 미사용 import·변수·함수는 바로 제거
- **변경 근거 테스트**: 로직 변경·API 수정은 사용자 요청으로 직접 설명 가능해야 한다. 설명 못 하는 줄은 되돌린다
- **기존 파일 전체 재작성 금지**: 디스크 파일을 통째로 갈아엎기 전 현재 내용을 Read 한 뒤 Edit 로 필요한 부분만 고친다. 내용을 가정한 채 Write 전체 덮어쓰기 금지. "복원하면 된다"고 단정 말고 `git status`·`git log -- <파일>` 로 추적·커밋 여부 먼저 확인 — untracked·uncommitted 파일은 덮어쓰면 복구 불가

## 코드 규칙
- 같은 파일의 여러 위치를 수정할 때는 한 응답 안에 Edit 호출을 모아서 처리한다 (동일 문자열 반복 치환은 `replace_all` 사용)
- 하드코딩 금지 → config/env 사용, 파일 경로는 pathlib
- 암묵적 동작 금지 (side effect, implicit conversion)
- 외부 입력 반드시 검증, 파괴적 작업 기본 비활성화
- 새 라이브러리 추가 시: 도입 이유·대안·선택 근거 명시
- 인터페이스 계약 명시: 입력/출력·shape/dtype·단위/범위

## 인덱스 / 매핑
- 정의부 먼저 확인, 코드 순서 ≠ 실제 값
- 인덱스 직접 접근 금지 → Enum + Dict 사용 (Single Source of Truth)
- 외부 인덱스와 내부 인덱스 분리 유지
- 위험 신호: `NAMES[idx]` 직접 접근, `if idx==0` 하드코딩, 외부 인덱스 그대로 사용

```python
from enum import IntEnum
class SensorId(IntEnum): TEMP=0, PRESSURE=2  # 외부 인덱스는 비연속일 수 있다
SENSOR_TO_METRIC = {SensorId.TEMP: Metric.CELSIUS, ...}
```

## ML 도메인
- 입력/출력 명시 (shape, dtype, 단위)
- 랜덤 시드 고정
- 전처리 / 모델 / 후처리 분리
- 모델 출력 인덱스 문서화, 출력 의미 명시 (logits/probability, threshold)
- 인덱스 매핑·변환 로직은 단위 테스트 필수 (경계값·예외 포함)

## 성능 / 자원 효율

- **운영 규모 기준으로 생각한다**: 10건 테스트에서 통과한 코드가 10,000건에서 OOM·타임아웃날 수 있다. 루프·배치·스트리밍 경계를 설계 시점에 확인한다.
- **메모리 누수 유발 루프 패턴 금지**: 아래 구조는 작성 전 반드시 점검한다.
  - 루프 안에서 결과 객체·텐서·파일 핸들을 리스트에 append 하고 루프 밖에서 한꺼번에 처리 → 배치 단위로 분리하거나 제너레이터로 전환
  - 루프 안에서 대용량 객체를 생성하고 `del` / `close` 없이 다음 반복으로 넘어가는 패턴 → 명시적 해제 또는 `with` 블록 사용
  - 전체 데이터를 메모리에 올린 후 순회하는 패턴 (`list(dataset)` 등) → 스트리밍/청크 처리로 전환
  - 의심스러운 루프를 작성했다면 "N이 10배 커지면 메모리가 몇 배 늘어나는가"를 먼저 답한다. 선형 이상이면 구조를 바꾼다.
- **병목 먼저 측정, 최적화 나중에**: 추측으로 최적화하지 않는다. 느리거나 메모리가 문제가 되면 프로파일링 결과를 먼저 확인한다.
- **운영 규모 검증은 완료 기준**: 소규모 sanity check 통과만으로 완료 판정 금지. 실제 입력 규모에서 메모리·시간을 1회 이상 실측한다.

## 토큰 관리
- 사전 확인 후 진행: 1000줄↑ / 5MB↑ / 이미지·PDF·데이터셋 / `**/*` 전체 검색
- 예외: 사용자 명시 요청, 일반 코드 검색
